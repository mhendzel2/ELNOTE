# Object-Storage Drill Artifacts

Timestamped object-storage validation artifacts are written here by `scripts/run_object_storage_drill.sh`.

Each artifact captures:
- Sample attachment metadata used for the drill
- Optional restore command execution evidence
- API login + signed URL issuance status
- Signed URL retrieval verification status
